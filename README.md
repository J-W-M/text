//打开终端，进入你的项目文件夹，初始化 Git（未初始化时执行）
git init
//绑定远程仓库
git remote add origin https://github.com/J-W-M-M/text.git
//创建.gitignore并写入模板
//提交并推送代码
git add .
git commit -m "初始化项目，添加gitignore"
git push -u origin main
